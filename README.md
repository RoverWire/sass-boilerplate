# Grunt + SASS + HAML Boilerplate Setup

just clone this repository, change name, version, author and description to fit your project. Then just type

		$ bundle install
		$ npm install
		$ grunt watch

Note: Ruby is needed to install sass gems
